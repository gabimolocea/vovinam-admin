"""
Theme setup script removed.

This project no longer supports the FrontendTheme feature. The original
`create_default_theme.py` script created a FrontendTheme in the database.

The file is intentionally left as a harmless placeholder to avoid accidental
execution that imports the removed model.
"""